import { defineConfig } from 'vitepress';

// https://vitepress.dev/reference/site-config
export default defineConfig({
	lang: 'en-US',
	title: 'Svelte Clerk',
	description: 'Clerk composables and components for Svelte',
	lastUpdated: true,
	cleanUrls: true
});
